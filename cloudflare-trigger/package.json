{
  "name": "inframind-motion-trigger",
  "version": "1.0.0",
  "description": "Cloudflare Worker that fires every 5 min and triggers the MOTION refresh GitHub workflow. Sidesteps GitHub's unreliable scheduled cron.",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "secret:pat": "wrangler secret put GITHUB_PAT"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
